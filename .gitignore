/target/
/campaigns/**/target/
/results/*.jsonl
/results/*.csv
/results/*.json
/results/*.environment.txt
.DS_Store

# Captured result files are ignored until a run has been reviewed. Curated
# manifests and summaries can be force-added deliberately.

# Captured campaign result data (add curated results deliberately)
campaigns/**/results-*.csv
/results/paper-*/
.venv/

# Cargo.lock is not tracked: requirements are carets at minor granularity, so a
# lockfile would pin what the carets exist to let float, and would silently
# decide which WorkTable a benchmark measured.
Cargo.lock
