# Results

Raw local outputs are ignored until reviewed. A curated result should include:

- raw JSONL for every repetition;
- the exact command and environment manifest;
- suite, WorkTable, and baseline commits;
- a machine-readable summary generated from all repetitions;
- notes for any invalidated or repeated run.

